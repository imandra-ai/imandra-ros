(jbuild_version 1)

(executable
  ( (name        test)
    (public_name test)
    (preprocess (pps (ppx_deriving_rpc)))
    (modules (test))
    (libraries 
      ( rpclib
        rpclib-lwt
        cohttp-lwt-unix
      ) 
    ) 
  )
)


