(jbuild_version 1)

(executable
  ( (name        test)
    (public_name test)
    (preprocess (pps (ppx_deriving_rpc)))
    (modules (test))
    (libraries 
      ( rpclib
        rpclib-lwt
        cohttp-lwt-unix
      ) 
    ) 
  )
)

(library
  ( (name        rosocaml)
    (public_name rosocaml)
    (wrapped     false)
    (modules (master_api))
    (preprocess (pps (ppx_deriving_rpc)))
    (libraries 
      ( rpclib
        rpclib-lwt
        cohttp-lwt-unix
      ) 
    ) 
  )
)
