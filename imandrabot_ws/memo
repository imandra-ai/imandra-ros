source /opt/ros/galactic/setup.bash
# Resolve dependencies
rosdep install -i --from-path src --rosdistro galactic -y
# Build
colcon build
# Setup env
source install/local_setup.sh
# Run webots stuff
ros2 launch imandrabot robot_launch.py

